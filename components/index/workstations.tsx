import scss from 'styles/components/index/workstations.module.scss';

type WorkStation = {
  type: "desktop" | "laptop";
};

const Component = ({ stations }: { stations: WorkStation[] }) => <></>;

export default Component;
