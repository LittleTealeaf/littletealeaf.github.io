name: "Pull Request Labeler"
on: [pull_request_target]



jobs:
  label_pull_request:
    if: github.repository_owner == 'LittleTealeaf'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v4
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
        sync-labels: true
