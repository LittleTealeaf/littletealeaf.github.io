const Page = ({}) => {
  const resume = require('content/resume.json');
  return <></>;
};

export const getStaticProps = ({ params }) => {
  return {
    props: {
    },
  };
};

export default Page;
